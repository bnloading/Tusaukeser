{
    "buildCommand": "npm run build",
    "outputDirectory": "build",
    "public": true,
    "routes": [
        {
            "src": "/images/(.*)",
            "dest": "/images/$1"
        }
    ]
}